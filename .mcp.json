{
  "mcpServers": {
    "entropy-data": {
      "type": "http",
      "url": "https://app.entropy-data.com/mcp",
      "headers": {
        "Authorization": "Bearer ${ENTROPY_DATA_API_KEY}"
      }
    }
  }
}
